source 'https://rubygems.org'

gem 'jekyll', '~> 4.2'
# github-pages gem pins a specific supported set; optional — we'll use jekyll directly for build
# gem 'github-pages', group: :jekyll_plugins

# Remote theme support
gem 'jekyll-remote-theme'
gem 'jekyll-feed'
gem 'jekyll-seo-tag'
